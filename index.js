// index.js

// Function expression for divide
function divide(a, b) {
    return a / b;
}

// Arrow function for square
const square = (x) => {
    return x * x;
};

// Arrow function for add
const add = (x, y) => {
    return x + y;
};
